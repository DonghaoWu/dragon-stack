import React from 'react';

const FilterSideBar = ({ logout, accountInfo }) => {
    return (
        <div className='filter-side-bar'>
            Filter side bar.
        </div>
    )
}

export default FilterSideBar;
